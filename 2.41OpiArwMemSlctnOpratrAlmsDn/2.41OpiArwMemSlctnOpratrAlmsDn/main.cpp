#include<iostream>
#include "claudia.h"
using namespace std;




int main() {


	//Declare a claudia object.
	//Code:

	//Declare class object pointer named claudiaPointer that points to memory address of claudia object
	//CODE:

	//Call the member function using the dot (.) operator on claudia object.
	//CODE:

	//Arrow operator dereferences pointer object and accesses the member in one step
	//Call SAME function using the arrow (->) operator on the pointer.
	//CODE:

	//---Extra Credit: Pointers & Dereferencing---

	//Declare int var x and initialize it to 42
	//CODE:

	//"p" is a pointer that stores the memory address of x.
	//Declare an int pointer p and set it equal to the memory address of x
	//CODE:

	//Output statement the displays p to then screen. "p equals: "
	//CODE:

	//Output statement to be printed to the screen that prints the pointer p.
	//CODE:

}