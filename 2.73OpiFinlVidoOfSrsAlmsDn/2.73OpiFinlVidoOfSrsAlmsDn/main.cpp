#include <iostream>
#include <string>
using namespace std;



int main() {


	//Instantiate string object named s1 of string class with arg sentence about your 
	//understanding which is basically nothing.
	//CODE:

	//Output to show s1.
	//CODE:


	//Use replace() to take out word in sentence and replace it with "OPPIE"
	//s1.replace function contains 3 parameters. 1. How many elements to count 2. followed by how many
	//elements of substring we are replacing 3. And what to replace those elements/characters with.
	//CODE:
	
	//Call the replace() again with arguments 17, 4,"KANOPI" on the s1 object. Then see the new
	//sentence to the screen (s1 object).
	//CODE:

	//insert() contains two parameters. 1. Number of elements
	//to count. 2. What to insert into the string at index location whatever you want.

	//Call the insert() function with arguments with whatever you want.
	//CODE:

	//Call the replace() function again with arguments 0, 5, "Adam" on the s1 object and then add
	//a cout statement to show that sentence (s1 object) to the screen.
	//CODE:

}