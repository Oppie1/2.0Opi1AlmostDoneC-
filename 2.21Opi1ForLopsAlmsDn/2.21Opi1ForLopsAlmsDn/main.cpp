#include <iostream>
using namespace std;




int main()
{


	cout << "Here we use a for loop that will print numbers (5-25). Then will go to next step in program." << endl;

	//Use for loop that declares and initializes int x to 0 and uses less than in control to list numbers 0-25
	//that increments by 5. Display output to screen during each iteration of loop.
	//CODE:

	cout << "The for loop has fully executed and you are at next step in main()" << endl;

	//Out x to screen each iteration.
	//CODE:

	//Output message to notify user for loop fully executed and they are back in main.
	//CODE:

}