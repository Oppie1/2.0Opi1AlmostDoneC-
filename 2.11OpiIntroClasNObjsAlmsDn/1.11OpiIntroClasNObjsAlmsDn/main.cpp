#include <iostream>
using namespace std;




//Declare a class named AdamClass
//CODE:

//Declare a public class access specifier.
//CODE:

//Declare string variable called name initialized to "Adam Tindall". REMEMBER we need semi colon (;) at end of class braces };
//CODE:

//Declare void function with no parameters named helloObject() that outputs welcome to AP intro to CS and name is...
//CODE:

int main() {

	//Create/instantiate object1 and object2 -> generic names from Adam Class
	//CODE:

	//Use object1 to call function we created outside of main. Use object2 to 
	//to call the name variable. Then output to screen at same time using cout
	//to produce a complete sentence.
	//CODE:

	cout << "\n-----------Bonus----------\n" << endl;
	cout << "You can create an object that  does two things at once:" << endl;

	//Use object (here object3) that can hold two pieces of information that can be used. 
	//Create/instantiate object, use SAME object to call helloObject() and outputs name to screen.
	//CODE:

	return 0;

}