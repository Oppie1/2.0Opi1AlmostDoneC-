#include <iostream>
#include<cstdlib>
#include <ctime>
using namespace std;



int main()
{

	//Call srand() that seeds the random number generator using time().
	//CODE:

	//Output statement saying the following are 9 unique numbers.
	//CODE:
	cout << "The following 9 numbers are unique" << endl;

	//Use for loop that initializes int x to 1 and prints 9 random numbers to the screen.
	//CODE:

	//Output sentence with expression calling rand() and % 6. Add 1 to compensate for instances with no remainder (0).
	//CODE:

	//Output statement for loop has fully executed and this is the next step in program.
	//CODE:
	cout << "The loop has fully executed and we are back in main." << endl;

}